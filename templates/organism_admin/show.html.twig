{% extends 'base.html.twig' %}

{% block title %}OrganismAdmin{% endblock %}

{% block body %}
    <h1>OrganismAdmin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organism_admin.id }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ organism_admin.logo }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ organism_admin.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ organism_admin.email }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ organism_admin.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organism_index') }}">back to list</a>

    <a href="{{ path('app_organism_edit', {'id': organism_admin.id}) }}">edit</a>

    {{ include('organism_admin/_delete_form.html.twig') }}
{% endblock %}
